{
  "qa": {
    "ch1": [
      {
        "id": "ch1-q1",
        "difficulty": "core",
        "tags": ["amdahl", "performance"],
        "q": "State Amdahl's Law and explain why it limits the speedup from optimizing only part of a program.",
        "a": "Speedup_overall = 1 / ((1 - F) + F/S), where F is the fraction of execution time that benefits from the enhancement and S is the speedup of that fraction. The law's key insight is that the unaffected fraction (1 - F) sets a hard ceiling on total speedup. Even with infinite speedup of the enhanced portion (S → ∞), overall speedup is capped at 1/(1 - F). Example: if you make 50% of execution 10× faster, overall speedup is only 1/(0.5 + 0.05) = 1.82×, not 10×. Architects use Amdahl's Law to prioritize the common case — optimizations on rarely-used hardware return little. This is why architects say 'make the common case fast.'"
      },
      {
        "id": "ch1-q2",
        "difficulty": "core",
        "tags": ["performance", "iron-law"],
        "q": "Write the CPU performance equation (Iron Law) and explain what each term tells the architect.",
        "a": "CPU time = Instruction Count × CPI × Clock Cycle Time. Instruction count is set by ISA design and compiler — architects rarely move it. CPI (cycles per instruction) is the architecture's playground: pipelining, caches, branch prediction, multiple issue all attack CPI. Clock cycle time is determined by circuit design, technology node, and the longest pipeline stage. The equation is multiplicative, so a 20% improvement in any one term improves CPU time by 20%. The Iron Law also exposes tradeoffs: deeper pipelines reduce cycle time but increase CPI (more hazards, branch mispredictions). Most ISA decisions affect at least two terms simultaneously."
      },
      {
        "id": "ch1-q3",
        "difficulty": "core",
        "tags": ["power", "energy"],
        "q": "Explain the difference between dynamic power, static power, and short-circuit power in a CMOS circuit. Which dominates in modern processors?",
        "a": "Dynamic power: P_dyn = ½ × C × V² × f × α, where C is switching capacitance, V is supply voltage, f is frequency, α is activity factor. This is the power burned charging and discharging gate capacitances. Static (leakage) power: current that flows even when transistors are not switching, dominated by subthreshold leakage and gate leakage. Short-circuit power: brief conduction during input transitions when both NMOS and PMOS are partially on; usually <10% of total. In older nodes (90nm+), dynamic power dominated. At sub-22nm, static power can rival dynamic power because shrinking V_t increases leakage exponentially. This is why the industry pivoted to multicore — voltage scaling stalled, so f could not keep climbing without melting chips. Modern designs aggressively use power gating, clock gating, and DVFS to manage both."
      },
      {
        "id": "ch1-q4",
        "difficulty": "core",
        "tags": ["benchmarks", "spec"],
        "q": "What are SPEC benchmarks, and why do architects prefer geometric mean to arithmetic mean when summarizing them?",
        "a": "SPEC (Standard Performance Evaluation Corporation) maintains industry-standard benchmarks like SPEC CPU (integer + floating-point), SPECpower, and SPECjbb. They use real applications run on real hardware to compare systems fairly. Architects use geometric mean because SPEC reports normalized speedups against a reference machine, and geometric mean is the only summary that is independent of the reference choice — it doesn't change if you renormalize. Arithmetic mean would be skewed by outlier benchmarks: a 100× speedup on one benchmark would dominate the average. Geometric mean treats a 2× speedup and a 0.5× slowdown as offsetting (their product is 1), which matches our intuition that a balanced design should not let any one benchmark dominate."
      },
      {
        "id": "ch1-q5",
        "difficulty": "advanced",
        "tags": ["dependability", "availability"],
        "q": "Define MTTF, MTTR, and availability. How do these connect for a system with redundancy?",
        "a": "MTTF (Mean Time To Failure) is the expected time until a component fails. MTTR (Mean Time To Repair) is the expected time to fix it. Availability = MTTF / (MTTF + MTTR). For a 'five nines' system (99.999%), allowable downtime is ~5 min/year. With redundancy, MTTF improves dramatically because the system tolerates individual failures: for two redundant components with independent failures, system MTTF ≈ MTTF² / (2 × MTTR). Module availability multiplies for serial systems (any failure brings down the system) but adds in parallel systems (both must fail). RAID, ECC memory, and dual power supplies are practical examples. WSCs use redundancy at every level because at scale, failures are routine — Google plans for the daily failure of disks rather than treating each as exceptional."
      },
      {
        "id": "ch1-q6",
        "difficulty": "advanced",
        "tags": ["cost", "wafer", "yield"],
        "q": "Walk through the IC cost equation. Why do larger dies become exponentially more expensive?",
        "a": "Cost_die = Cost_wafer / (Dies_per_wafer × Yield). Dies per wafer scales roughly as wafer_area / die_area. Yield follows Bose-Einstein: Yield = (1 + (defects_per_area × die_area)/N)^(-N), where N is process complexity. The killer is the exponential in yield: doubling die area more than doubles defects per die, dropping yield sharply. For a 1 cm² die at 0.04 defects/cm², yield might be 70%; double the area to 2 cm² and yield can drop to ~50%. Combined with fewer dies per wafer, the cost per die can grow 3-4× when die area doubles. This is why architects fight to fit designs in smaller dies, why chiplets are popular (smaller dies have higher yield), and why GPUs with 600+ mm² dies are so expensive."
      }
    ],
    "ch2": [
      {
        "id": "ch2-q1",
        "difficulty": "core",
        "tags": ["cache", "3cs"],
        "q": "What are the 3 Cs of cache misses? What's the 4th C, and when did it become important?",
        "a": "The 3 Cs: Compulsory (first reference to a block — unavoidable), Capacity (working set exceeds cache size), and Conflict (set associativity insufficient — multiple addresses map to same set). The 4th C is Coherence misses, which appear in multicore systems when one core invalidates another's cached copy via the coherence protocol. Coherence misses became dominant in the multicore era (mid-2000s onward) because previously, single-core systems had no coherence traffic. Diagnostic uses: increasing block size attacks compulsory misses (spatial locality) but worsens conflict misses; increasing associativity attacks conflict misses; increasing total size attacks capacity misses. False sharing is a coherence-miss pattern where two cores write different bytes in the same line and bounce the line back and forth — a classic multicore performance bug."
      },
      {
        "id": "ch2-q2",
        "difficulty": "core",
        "tags": ["cache", "amat"],
        "q": "Write the AMAT equation and use it to explain why a multi-level cache hierarchy works.",
        "a": "AMAT = Hit_time + Miss_rate × Miss_penalty. For a multi-level hierarchy: AMAT = Hit_L1 + Miss_rate_L1 × (Hit_L2 + Miss_rate_L2 × Miss_penalty_L2). The hierarchy works because each level filters traffic to the next: L1 absorbs most accesses fast, L2 catches L1 misses with moderate latency, L3 catches the rare misses that escape L2, and DRAM only sees the residual. The L1 stays small to keep hit_time low (critical for clock frequency), L2/L3 grow larger to lower miss rates. Without a hierarchy, you'd be forced to choose: a small fast cache (lots of misses) or a large slow cache (every hit costs 20+ cycles). The hierarchy gives you both fast hits and high overall hit rate."
      },
      {
        "id": "ch2-q3",
        "difficulty": "core",
        "tags": ["cache", "write-policy"],
        "q": "Compare write-through vs write-back. When would you choose each, and what is a write buffer?",
        "a": "Write-through: every write goes to both cache and lower memory. Pro: simple, always consistent, no dirty bit needed, easy for I/O coherence. Con: high memory bandwidth — every write hits the bus. Write-back: writes update cache only; the dirty block is written to memory on eviction. Pro: low bandwidth (bursts on eviction, batched writes). Con: needs dirty bit, on a miss you may have to write back the victim before fetching, complicates coherence. Real designs almost always use write-back for L1/L2 (bandwidth is precious) and write-through for very small caches or strict coherence domains. A write buffer (a.k.a. store buffer) sits between the cache and memory, holding pending writes so the CPU doesn't stall on each store. With write-merging, multiple writes to the same line coalesce. Hazard: a load that hits the buffer must check the buffer first (store-to-load forwarding), or you read stale data."
      },
      {
        "id": "ch2-q4",
        "difficulty": "advanced",
        "tags": ["cache", "optimizations"],
        "q": "Name five of Hennessy & Patterson's ten advanced cache optimizations and what each targets.",
        "a": "(1) Small and simple L1 caches — keeps hit_time small, supports high clock. (2) Way prediction — predicts which way of a set holds the data, gets most of associative hit rate at direct-mapped speed. (3) Pipelined cache access — overlaps cache access with next-cycle work; common in modern L1s. (4) Non-blocking caches (hit-under-miss, miss-under-miss) — service hits while a miss is outstanding; essential for OoO cores. (5) Multibanked caches — multiple banks accessed in parallel for higher bandwidth; standard in L2/L3. Others include critical word first/early restart, merging write buffer, compiler prefetching, hardware prefetching, and trace caches. The 10 optimizations group into four categories: reduce hit time, reduce miss rate, reduce miss penalty, increase bandwidth."
      },
      {
        "id": "ch2-q5",
        "difficulty": "advanced",
        "tags": ["virtual-memory", "tlb"],
        "q": "Explain the role of the TLB. What happens on a TLB miss vs a page fault?",
        "a": "The TLB (Translation Lookaside Buffer) is a small cache of recent virtual-to-physical page translations, typically 32-1024 entries, fully associative or highly set-associative. On a memory access, the CPU sends the virtual page number to the TLB; if it hits, the physical page is returned in 1-2 cycles and the access proceeds. On a TLB miss, the CPU walks the page tables (hardware walk on x86, software walk on MIPS) to find the translation; this is fast (tens of cycles, typically) because the page tables are usually in cache. On a page fault, the page table entry is marked invalid because the page is not in physical memory at all; the OS gets an exception, swaps in the page from disk (millions of cycles), updates the page table, and restarts the instruction. TLB miss is hardware-handled and microsecond-scale; page fault is OS-handled and millisecond-scale. Modern CPUs have multi-level TLBs (L1 ITLB/DTLB, then a unified L2 TLB) and 'huge pages' (2MB, 1GB) to reduce TLB pressure for memory-hungry workloads."
      },
      {
        "id": "ch2-q6",
        "difficulty": "advanced",
        "tags": ["virtual-machines", "vm"],
        "q": "What protection issues do virtual machines solve, and how does hardware virtualization (Intel VT-x, AMD-V) help?",
        "a": "Virtual machines (VMs) provide isolation: each guest OS sees a complete machine, but the hypervisor (VMM) controls the physical hardware. Without hardware support, the VMM uses techniques like binary translation or paravirtualization to trap and emulate sensitive instructions. The classical x86 ISA had ~17 'sensitive but not privileged' instructions that did not trap when the guest ran in user mode, breaking pure virtualization (Popek-Goldberg requirements). Intel VT-x and AMD-V add a new ring (root/non-root mode) and VMCS (VM Control Structure), so the hypervisor can configure which guest events trap. Extended page tables (EPT/NPT) handle two-level address translation in hardware: guest-virtual → guest-physical → host-physical, eliminating the shadow page table software overhead. Result: near-native performance for VMs, which underpins all modern cloud computing (AWS EC2, Azure, Google Cloud)."
      }
    ],
    "ch3": [
      {
        "id": "ch3-q1",
        "difficulty": "core",
        "tags": ["dependences", "ilp"],
        "q": "Explain the three types of data dependences (RAW, WAR, WAW). Which is a 'true' dependence?",
        "a": "RAW (Read After Write): instruction j reads a value written by instruction i. Example: i: ADD R1, R2, R3; j: SUB R4, R1, R5. j needs i's result. This is a true (flow) dependence — no renaming can remove it without changing semantics. WAR (Write After Read, anti-dependence): j writes to a register read by i. Example: i: ADD R4, R1, R2; j: SUB R1, R5, R6. WAR exists only because the register name is reused; renaming R1 in j to a fresh physical register removes the dependence. WAW (Write After Write, output dependence): both i and j write the same register. Same fix as WAR — register renaming. Only RAW is fundamental; WAR and WAW are 'name dependences' that come from the limited architectural register namespace. Tomasulo's algorithm and modern OoO cores eliminate WAR/WAW via register renaming, exposing far more ILP than the original code suggested."
      },
      {
        "id": "ch3-q2",
        "difficulty": "core",
        "tags": ["tomasulo", "ilp"],
        "q": "Walk through Tomasulo's algorithm. What problem does it solve, and what are the three main hardware structures?",
        "a": "Tomasulo's algorithm (IBM 360/91, 1967) enables out-of-order execution with register renaming, eliminating WAR and WAW hazards and allowing instructions to execute as soon as their operands are ready. Three main structures: (1) Reservation Stations — buffer instructions waiting for operands; one set per functional unit. Each station holds the opcode, source operands (or tags pointing to producing reservation stations), and the destination tag. (2) Common Data Bus (CDB) — when a functional unit completes, it broadcasts result + tag on the CDB; all reservation stations and registers waiting on that tag snarf the value simultaneously. (3) Register Status Table — for each architectural register, holds either 'no pending writer' or 'value will come from reservation station X.' Issue stage: dispatch to reservation station, rename source operands using register status table, mark destination as pending. Execute: when all sources arrive, send to FU. Write Result: broadcast on CDB, update register file if no newer writer is pending. The algorithm naturally handles loops via continued issue and provides the foundation for all modern OoO cores."
      },
      {
        "id": "ch3-q3",
        "difficulty": "core",
        "tags": ["rob", "speculation"],
        "q": "What is a Reorder Buffer, and how does it enable precise exceptions in a speculative OoO processor?",
        "a": "The Reorder Buffer (ROB) is a circular buffer that holds instructions in program order from issue until commit. Tomasulo's original algorithm executed and wrote back out-of-order, making exceptions imprecise (you can't tell which instructions had completed). Adding a ROB separates 'execute and write result' from 'commit'. Instructions execute out-of-order and write results to the ROB entry, but commit (architectural state update) happens in program order from the ROB head. On an exception, the processor flushes all ROB entries past the offending instruction and squashes their effects — architectural state is exactly as if execution had stopped there. The ROB also enables speculation: branch predictions execute past unresolved branches into ROB entries; if the branch resolves wrong, those entries are flushed without ever updating architectural state. ROB sizes have grown from ~40 entries (Pentium Pro) to 350+ (modern Intel/AMD/Apple) because more in-flight instructions = more ILP exposed. Apple's M-series cores famously have very large ROBs (~600+ on M1)."
      },
      {
        "id": "ch3-q4",
        "difficulty": "core",
        "tags": ["branch-prediction"],
        "q": "Compare 1-bit, 2-bit, correlating (m,n), and tournament branch predictors. Why has accuracy mattered so much more in modern processors?",
        "a": "1-bit predictor: stores last outcome. Mispredicts twice on every loop exit (once entering, once exiting steady state). 2-bit (bimodal) predictor: 4-state saturating counter — must mispredict twice to flip prediction. Far better for loops; ~85-90% on SPEC. Correlating (m,n) predictor: uses m bits of global branch history (last m branches) to index into 2^m tables of 2-bit counters. Captures branch correlations like 'if branch A taken, branch B usually taken too.' Achieves ~95%+. Tournament predictor: combines a local predictor (per-branch history) with a global predictor (global history) and uses a meta-predictor to choose which one to trust per branch. Used in Alpha 21264. Modern processors use TAGE or perceptron predictors achieving 97-99% accuracy. Why it matters: a deeply pipelined OoO core with 100+ in-flight instructions wastes everything between the branch and resolution on a misprediction. With 20-stage pipelines and 4-wide issue, one mispredict costs 80 instructions of work. At 95% accuracy, mispredict cost is bearable; at 90%, performance collapses."
      },
      {
        "id": "ch3-q5",
        "difficulty": "advanced",
        "tags": ["smt", "multithreading"],
        "q": "Explain coarse-grained, fine-grained, and simultaneous multithreading. Why is SMT (Hyper-Threading) so common today?",
        "a": "Coarse-grained MT: switches threads only on a long stall (e.g., L2 miss). Pro: minimal hardware overhead. Con: short stalls (RAW hazards, branch mispredictions) waste cycles. Fine-grained MT: round-robin between threads every cycle. Pro: hides all stalls. Con: each thread runs slower because it gets only 1/N cycles; complicates branch prediction. Used in Sun UltraSPARC T1 ('Niagara'). Simultaneous Multithreading (SMT): multiple threads issue instructions in the same cycle, sharing functional units, ROB, register file. Built on top of OoO superscalar — the existing renaming and scheduling hardware already tolerates independent instruction streams; SMT just adds per-thread architectural register sets and ROB partitions. SMT is common (Intel Hyper-Threading, AMD SMT, IBM POWER 4-8 way) because it costs only ~5% area for ~25-40% throughput gain by filling functional unit gaps left by single-thread execution. Apple M-series notably skips SMT, preferring big single-threaded cores. Tradeoff: SMT can hurt performance for cache-sensitive workloads (threads compete for cache) and creates security concerns (Spectre-class side channels)."
      },
      {
        "id": "ch3-q6",
        "difficulty": "advanced",
        "tags": ["limits-of-ilp", "speculation"],
        "q": "What did the Hennessy & Patterson 'limits of ILP' studies find, and why does it matter for modern processor design?",
        "a": "The limits-of-ILP studies (originally from Wall, MIT, and later in CAAQA) varied window size, branch prediction accuracy, register renaming, and memory aliasing assumptions on real benchmarks to find theoretical ILP ceilings. Key findings: with a perfect predictor and unlimited window, integer programs show ~5-10 ILP, FP programs ~10-30 ILP. With realistic branch prediction (95-97%), integer ILP drops to 4-6. With realistic window sizes (~256), ILP drops further. Even with everything ideal, integer programs hit a ceiling around 10. Why this matters: it explains why processors stalled at 4-6 wide issue. Going wider doesn't help when programs simply don't have more parallelism to extract. Branch prediction and memory disambiguation are the gating factors. The industry's response: pivot from ILP to thread-level parallelism (multicore, SMT) and data-level parallelism (SIMD, GPU) — the work that ends up in Chapters 4 and 5. The studies also justify aggressive speculation: you must prefetch, predict, and execute speculatively because waiting is fatal."
      }
    ],
    "ch4": [
      {
        "id": "ch4-q1",
        "difficulty": "core",
        "tags": ["dlp", "vector"],
        "q": "What is the difference between vector processors, SIMD instruction set extensions, and GPUs? What problem do they all solve?",
        "a": "All three exploit data-level parallelism (DLP) — many independent operations on different data elements. Vector processors (Cray-1, modern AVX-512 in vector mode): a single instruction operates on a vector register (e.g., 64 elements). One fetch/decode amortizes across many operations. Pipelined vector functional units run hot. Memory uses unit-stride or strided access patterns. SIMD instruction set extensions (MMX → SSE → AVX → AVX-512 on x86; NEON on ARM): packed-data registers (128/256/512 bits) with operations that act on all lanes simultaneously. Compared to vectors: shorter vectors (often 4-16 elements), no vector mask register history initially, fits into a scalar OoO pipeline. GPUs: massively parallel SIMT (Single Instruction Multiple Thread) execution. NVIDIA: 32-thread warps execute in lockstep; the GPU has thousands of lanes across many SMs. Memory hierarchy is bandwidth-optimized (HBM, GDDR). The difference vs. vector: GPUs use threads as the abstraction, with hardware warps doing the SIMD execution underneath. All three answer: 'how do I run the same computation on huge arrays without paying instruction-fetch overhead per element?'"
      },
      {
        "id": "ch4-q2",
        "difficulty": "core",
        "tags": ["roofline", "performance"],
        "q": "Explain the Roofline performance model. How is it used to diagnose whether a kernel is compute-bound or memory-bound?",
        "a": "Roofline model (Williams, Waterman, Patterson 2009): plots achievable performance (GFLOPS) vs operational intensity (FLOPs per byte of memory traffic) on a log-log graph. Two ceilings: a sloped line (memory bandwidth × intensity) and a flat line (peak compute throughput). The 'roof' is the lower of the two. Kernels with low operational intensity (e.g., SAXPY, dense matrix-vector multiply) sit under the slope and are memory-bound; the only way to speed them up is to reduce memory traffic (blocking, cache reuse, fewer bytes per FLOP). Kernels with high intensity (dense matrix-matrix multiply with N³ FLOPs and N² loads) are compute-bound; SIMD width and FU count matter, memory tweaks don't help. Roofline immediately tells you which optimization to attempt. The model can be extended to multiple roofs (no SIMD, no FMA, etc.) showing what each missed optimization costs you. It is the standard performance-bug diagnostic in HPC and matches the way accelerator vendors design for 'right-sized' bandwidth-to-compute ratios."
      },
      {
        "id": "ch4-q3",
        "difficulty": "advanced",
        "tags": ["gpu", "simt"],
        "q": "Explain NVIDIA's SIMT execution model. What is a warp, and what happens on warp divergence?",
        "a": "SIMT (Single Instruction Multiple Thread) is NVIDIA's term for the GPU execution model: programmers write per-thread code, but the hardware groups 32 threads into a warp that executes one instruction in lockstep across 32 lanes. Threads in a warp share PC and instruction stream; each lane has its own registers, predicates, and memory pointers. The streaming multiprocessor (SM) issues one warp instruction per cycle (or more on later architectures), so 32 lanes do 32 work items per warp instruction — true SIMD execution under a thread-level programming model. Warp divergence: when threads in a warp take different paths at a branch (e.g., if (tid < 16) {A} else {B}), the warp can't execute both paths in lockstep. The hardware serializes: it executes A with the matching lanes active and B-lanes masked off, then executes B with the inverse mask. Total time = time(A) + time(B), and half the lanes are idle in each phase. Performance halves. Programmers structure code to keep threads in a warp on the same control path (often by restructuring data layout or using warp-aligned conditions). Newer architectures (Volta+) added independent thread scheduling within a warp, easing some divergence patterns."
      }
    ],
    "ch5": [
      {
        "id": "ch5-q1",
        "difficulty": "core",
        "tags": ["coherence", "mesi"],
        "q": "Explain the MESI cache coherence protocol. What problem does each state solve?",
        "a": "MESI maintains a single coherent view of memory across multiple caches. Each cache line is in one of four states: Modified (M) — only this cache has the line, value differs from memory; cache must write back on eviction; can be read/written without bus traffic. Exclusive (E) — only this cache has the line, but it matches memory; can transition to M silently on a write (no bus broadcast needed). Shared (S) — multiple caches may hold the line, all match memory; reads are local, but writes require invalidating other copies. Invalid (I) — line is not valid in this cache. Why each matters: M enables write-back caching with coherence. E is the optimization that makes MESI better than MSI: when you load a line and no one else has it, you go to E rather than S, avoiding a broadcast on the next write. S enables sharing for read-heavy data (e.g., shared lookup tables). I is the 'don't have it' state. Bus operations: BusRd (read miss), BusRdX (read-with-intent-to-modify), Invalidate (downgrade others to I), Flush (write back to memory). Variants: MOESI adds Owned (one cache is responsible for supplying data on snoop, eliminating memory writeback for shared dirty data); MESIF adds Forward (one designated cache forwards data on snoop, used in Intel QPI)."
      },
      {
        "id": "ch5-q2",
        "difficulty": "core",
        "tags": ["snooping", "directory"],
        "q": "When would you use a snooping coherence protocol vs a directory protocol? Why don't large multicore systems use snooping?",
        "a": "Snooping: every cache observes every bus transaction and reacts to messages it cares about. Pro: simple, low-latency for small systems. Con: every transaction is broadcast — bus bandwidth is O(N) but coherence traffic is O(N²) for N cores. Doesn't scale beyond ~16-32 cores on a shared bus. Directory protocols: a directory entry per memory block tracks which caches have copies and their states. Coherence messages are point-to-point, not broadcast. Pro: scales to hundreds/thousands of cores. Con: directory storage overhead (often 1-4% of memory), extra hop latency to consult the directory. Used in: SGI Origin, modern Intel/AMD server chips with 32+ cores, AMD Zen with chiplet-based CCXs. Modern hybrid: small clusters use snooping internally (e.g., a CCX of 8 cores), and clusters interconnect via a directory protocol (e.g., AMD Infinity Fabric, Intel mesh). Snooping bus → snooping ring → mesh+directory is the historical scaling path. Directory entry encoding: full bit-vector (one bit per cache, scales linearly), limited-pointer (k pointers, broadcasts when more than k sharers), or coarse-vector (one bit per group of caches)."
      },
      {
        "id": "ch5-q3",
        "difficulty": "advanced",
        "tags": ["consistency", "memory-models"],
        "q": "What is the difference between sequential consistency and a relaxed memory model? Give a real-world example of where it matters.",
        "a": "Sequential consistency (SC, Lamport 1979): the result of any execution is as if memory operations from all processors were placed in some total order, with each processor's operations in program order. Intuitive but expensive: forbids most reordering, so out-of-order CPUs and write buffers must drain before subsequent operations on shared data. Relaxed memory models: weaken some orderings to enable performance, requiring programmers to insert fences/barriers where ordering is needed. TSO (Total Store Order, x86): allows store-to-load reordering (load can pass an earlier store to a different address); all other orderings preserved. Weak memory models (ARM, POWER): allow most reorderings; require explicit barriers (DMB, DSB on ARM) for ordering. Real example: implementing a lock or message queue. With SC, simple flag-based handshaking works. With weak memory models, you must use acquire/release semantics (fence after lock acquire, fence before lock release) to ensure data writes become visible before the flag. This is why C++11 introduced std::memory_order_acquire / release / seq_cst — to expose memory model choices to programmers. Java's volatile and Go's atomics have similar mechanics. A subtle bug: x86 TSO is forgiving enough that lots of bad code works; ARM exposes the bugs immediately, which is why apps targeting Apple Silicon often hit memory-model crashes that didn't manifest on x86."
      },
      {
        "id": "ch5-q4",
        "difficulty": "advanced",
        "tags": ["false-sharing", "performance"],
        "q": "What is false sharing, and how do you fix it?",
        "a": "False sharing: two cores write to different bytes that fall on the same cache line, causing the line to bounce back and forth via the coherence protocol even though there's no actual data dependence. The cache, which tracks coherence at line granularity (typically 64 bytes), can't tell that the writes are independent. Symptom: a multithreaded program where each thread updates its own counter or accumulator runs much slower than expected — sometimes worse than single-threaded. Diagnose with hardware performance counters (LLC misses, HITM events on Intel). Fix: pad data structures so that per-thread fields land on different cache lines. In C/C++: align with alignas(64) or pad with char _padding[64-sizeof(field)]. Better: aggregate per-thread state into thread-local structures, only combining at the end. In Java: @Contended annotation. In Go: separate fields with cache-line-sized padding fields. Modern CPUs have 64-byte lines; some architectures (Apple M-series) have 128-byte lines, so padding heuristics need updating. False sharing is one of the most common multicore performance bugs and is invisible to most profilers — you have to look for it specifically."
      }
    ],
    "ch6": [
      {
        "id": "ch6-q1",
        "difficulty": "core",
        "tags": ["wsc", "datacenter"],
        "q": "What distinguishes a Warehouse-Scale Computer (WSC) from a traditional cluster or datacenter?",
        "a": "Hennessy & Patterson coined WSC to capture the fact that hyperscale facilities (Google, Amazon, Microsoft, Facebook) are designed as a single computer at the warehouse level, not collections of independent machines. Distinguishing characteristics: (1) Homogeneous hardware — tens of thousands of identical servers, identical storage, single network fabric, all designed together. (2) Single application owner — typically a single company runs all workloads, so no multi-tenancy isolation needed at the metal level. (3) Single administration domain — one team operates the whole facility. (4) Failure as routine — at scale (100K+ machines), several disks, NICs, power supplies fail every day; the software is built to tolerate this rather than treating each as a crisis. (5) Cost-driven design — every component is selected for $/performance, not absolute performance; the WSC is optimized as a system end-to-end including PUE (Power Usage Effectiveness), cooling, power distribution, and networking. (6) Programming model — frameworks like MapReduce, Spark, Borg/Kubernetes hide individual machine failures from the application. Result: WSCs achieve much better $/op than running the same workloads on enterprise hardware in a colo, which is why cloud computing exists economically."
      },
      {
        "id": "ch6-q2",
        "difficulty": "advanced",
        "tags": ["mapreduce", "wsc"],
        "q": "Walk through MapReduce. Why is it well suited to WSCs?",
        "a": "MapReduce (Dean & Ghemawat, OSDI 2004): a programming model for distributed batch processing. Programmer writes two functions: Map(k1, v1) → list((k2, v2)) and Reduce(k2, list(v2)) → list(v3). The framework partitions input across thousands of workers, runs Map in parallel, shuffles intermediate (k2, v2) pairs to the reducers based on k2, then runs Reduce. Example: word count — Map emits (word, 1) for each word, Reduce sums counts per word. Why it suits WSCs: (1) Embarrassingly parallel — each Map and Reduce task is independent, so any worker can execute any task. (2) Failure tolerance — if a worker fails, the framework reassigns its tasks to another worker (re-executing the Map or Reduce). (3) Locality — the scheduler runs Map tasks on the node where the input data lives (HDFS replicas), minimizing network traffic. (4) Hides distributed systems pain — programmers think in terms of two functions, not RPC, partitioning, or recovery. (5) Throughput over latency — designed for batch jobs taking minutes to hours, where occasional slow workers ('stragglers') are tolerable and handled by speculative execution. Modern frameworks (Spark, Flink, Beam) descend from MapReduce, extending it with in-memory caching and DAG execution but preserving the programming-model insight. WSCs are designed around these workloads — their high inter-node bandwidth, large local storage, and scheduler-friendly architecture all support MapReduce-style computation."
      }
    ],
    "appA": [
      {
        "id": "appA-q1",
        "difficulty": "core",
        "tags": ["isa", "risc-cisc"],
        "q": "Compare RISC and CISC ISAs. Why did RISC win, and why does x86 (CISC) still dominate desktops/servers?",
        "a": "RISC (MIPS, SPARC, ARM, RISC-V): fixed-length instructions, load-store architecture (memory only via dedicated loads/stores), few addressing modes, simple instruction semantics, large register file. Pro: easy to pipeline, easy to decode, easy to predict instruction count. CISC (x86, VAX, IBM 360): variable-length instructions, memory operands in arithmetic instructions, many addressing modes, complex instructions (REP MOVS, ENTER, LEAVE), small register file (in legacy x86). Pro: dense code, fewer instructions per task, backward compatible. Why RISC won technically: simpler decode means deeper pipelines, higher clock speeds, better branch prediction, easier OoO. Why x86 still dominates: (1) Backward compatibility with 30+ years of binaries — the largest software ecosystem in history. (2) Modern x86 cores translate CISC instructions into RISC-like micro-ops at decode, then run an OoO RISC backend underneath; the CISC ISA is effectively a compression scheme over an internal RISC core. (3) Intel and AMD have invested billions in process and microarchitecture, closing the gap. The recent shift: Apple Silicon (ARM/RISC), AWS Graviton (ARM), and rising RISC-V show that the RISC argument is winning again at the system level — but x86 is far from dead. ISA matters less than microarchitecture and ecosystem."
      }
    ],
    "appB": [
      {
        "id": "appB-q1",
        "difficulty": "core",
        "tags": ["cache", "associativity"],
        "q": "Explain direct-mapped, set-associative, and fully-associative caches. What are the tradeoffs?",
        "a": "Direct-mapped: each memory block maps to exactly one cache line, determined by index = (address / block_size) mod num_lines. Pro: simplest, fastest hit time, no associativity comparator. Con: high conflict miss rate; two hot addresses that collide on the same set will thrash. n-way set-associative: each block can go in any of n lines within its set. Index selects the set; the n tags in the set are compared in parallel. Pro: dramatically reduces conflict misses (8-way is often within 1% of fully-associative). Con: more comparators, higher hit time, more energy per access. Fully-associative: a block can go anywhere; every line's tag is compared in parallel. Pro: no conflict misses (every miss is capacity or compulsory). Con: O(N) comparators is expensive — only practical for small caches like TLBs (32-128 entries). Real designs: L1 caches are typically 4-8 way (balance hit time vs miss rate); L2/L3 are 8-16 way; TLBs are fully associative. Replacement policy matters more in higher associativity: LRU, pseudo-LRU, or RRIP (re-reference interval prediction) all common. Modern microarchitectures use way prediction in L1 to get near-direct-mapped hit time with set-associative miss rate."
      }
    ],
    "appC": [
      {
        "id": "appC-q1",
        "difficulty": "core",
        "tags": ["pipelining", "hazards"],
        "q": "Describe the 5-stage MIPS pipeline. What hazards arise and how are they handled?",
        "a": "Stages: IF (fetch), ID (decode + register read), EX (execute / address calc), MEM (memory access), WB (write back). One instruction enters per cycle; throughput is one instruction/cycle steady-state, latency is 5 cycles per instruction. Hazards: (1) Structural — two instructions need the same hardware. MIPS avoids this by separate I-cache and D-cache (Harvard architecture), and by dedicating each stage to specific hardware. Real-world structural hazard: shared FP divider when two divides issue back-to-back. (2) Data hazards — RAW dependences cause stalls when consumer enters EX before producer finishes WB. Solutions: forwarding/bypassing (route EX/MEM and MEM/WB pipeline register outputs back to EX inputs), eliminating most stalls. The remaining unavoidable case: load-use hazard — load result is available end of MEM, but a dependent instruction's EX needs it earlier; requires one stall cycle (resolved by software scheduling — the 'load delay slot'). (3) Control hazards — branches resolve in MEM (or ID with early branch resolution), but new instructions have already been fetched. Solutions: stall, predict-not-taken (squash if wrong), delayed branch (fill delay slot with useful work), branch prediction. The 5-stage pipeline is the canonical teaching example because every modern OoO processor's ALU pipe still looks like a deeper version of this with the same hazards and similar mitigations."
      },
      {
        "id": "appC-q2",
        "difficulty": "advanced",
        "tags": ["pipelining", "exceptions"],
        "q": "What's the difference between precise and imprecise exceptions, and why is precise the standard?",
        "a": "Precise exceptions: when an exception occurs at instruction i, all instructions before i have completed (architectural state updated), and no instruction at or after i has any visible effect. The PC saved is exactly i, so the OS can handle the exception and resume cleanly. Imprecise exceptions: some instructions after i have updated state (e.g., an instruction in WB committed while i in EX faulted), so architectural state is inconsistent with the exception PC. Resuming exactly is impossible without extra information. Why precise is required: (1) Page faults — must restart exactly the faulting load/store after page is brought in. (2) Debugging — single-step, breakpoint behavior must be predictable. (3) Virtual memory — paging requires precise restartable exceptions. (4) Trap-and-emulate FP — older systems trapped on FP and emulated in software. Implementation in pipelines: two main techniques. (1) Reorder buffer (used in OoO cores) — instructions execute out-of-order but commit in-order; on exception, flush ROB past the fault and use the in-order commit point. (2) Exception PC stacking with controlled stalls — for in-order pipelines, ensure later instructions don't write back before earlier ones could fault. Imprecise exceptions appeared briefly in early FP pipelines (Alpha 21164 imprecise FP) but were considered a regression; modern ISAs require precise exceptions for all exception classes."
      }
    ]
  }
}
